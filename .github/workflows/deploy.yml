name: Deploy to Firebase Hosting

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install deps
        run: npm ci

      # ВАЖНО: билд должен получить публичные ключи, иначе клиент не узнает куда ходить
      - name: Build (Next.js)
        run: npm run build
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}

      # Аутентификация к GCP по JSON сервисного аккаунта
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY_JSON }}

      - name: Install Firebase CLI
        run: npm i -g firebase-tools

      # Включаем веб‑фреймворки (Next.js на Firebase Hosting SSR)
      - name: Enable Firebase webframeworks
        run: firebase experiments:enable webframeworks

      # Деплой Hosting (без --site и прочего, берём из firebase.json)
      - name: Deploy to Firebase Hosting
        run: firebase deploy --only hosting
        env:
          # не обязательно, но полезно, если у тебя есть несколько аккаунтов
          FIREBASE_TOKEN: ""
